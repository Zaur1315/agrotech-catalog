<section class="section section-shell section-shell--muted" data-reveal><div class="site-container content-split content-split--reverse"><div class="content-split__media"><img src="{{ asset($section['image']) }}" width="1000" height="760" loading="lazy" alt="{{ $section['image_alt'] }}"><span class="image-overlay"></span></div><div class="content-split__body"><p class="section-eyebrow">{{ $section['eyebrow'] }}</p><h2 class="section-title">{{ $section['title'] }}</h2><p class="section-description">{{ $section['description'] }}</p><div class="contact-facts"><address>{{ config('site.contact.address') }}</address><a href="tel:{{ config('site.contact.phone_tel') }}">{{ config('site.contact.phone') }}</a></div><div class="home-hero__actions"><a class="btn-primary" href="{{ route($section['cta_route']) }}">{{ $section['cta_label'] }}</a><a class="btn-outline" href="{{ config('site.contact.maps_url') }}" target="_blank" rel="noopener">Get Directions</a></div></div></div></section>
